# Essential dependencies for Pricing App (Host-friendly)
# These packages are most likely to install successfully on shared hosting

# Core functionality
numpy_financial>=1.0.0
requests>=2.31.0
aiohttp>=3.8.0
beautifulsoup4>=4.12.0
lxml>=4.9.0
jinja2>=3.1.0
loguru>=0.7.0

# Data processing (try to install, may fail on some hosts)
pandas>=2.0.0
numpy>=1.24.0

# Visualization (lightweight)
plotly>=5.15.0

# Basic ML (may fail on resource-limited hosts)
scikit-learn>=1.3.0

# Web scraping (may need system dependencies)
selenium>=4.15.0
webdriver-manager>=4.0.0

# Background tasks (may not work on shared hosting)
celery>=5.3.0
redis>=5.0.0

# Note: Heavy packages like tensorflow, torch, etc. are excluded
# for hosting compatibility. Advanced ML features will be limited.
